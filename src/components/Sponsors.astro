---
const sponsors = [
  {
    name: "Host Wise",
    logo: "/images/sponsors/hostwise.svg",
    url: "https://hostwise.pt"
  },
  {
    name: "Guesty",
    logo: "/images/sponsors/guesty.svg",
    url: "https://guesty.com"
  },
  {
    name: "PriceLabs",
    logo: "/images/sponsors/pricelabs.svg",
    url: "https://pricelabs.co"
  },
  {
    name: "Airbnb",
    logo: "/images/sponsors/airbnb.svg",
    url: "https://airbnb.com"
  }
];
---

<section class="sponsors section" id="parceiros">
  <div class="container">
    <div class="sponsors-header">
      <span class="sponsors-label">Parceiros</span>
      <h2 class="section-title">Sponsors & Partners</h2>
      <p class="section-subtitle">
        As empresas que tornam este evento possível
      </p>
    </div>

    <div class="sponsors-grid">
      {sponsors.map((sponsor) => (
        <a href={sponsor.url} target="_blank" rel="noopener noreferrer" class="sponsor-card">
          <div class="sponsor-logo">
            <img src={sponsor.logo} alt={sponsor.name} />
          </div>
          <span class="sponsor-name">{sponsor.name}</span>
        </a>
      ))}
    </div>

    <div class="sponsors-cta">
      <p>Interessado em ser parceiro do evento?</p>
      <a href="mailto:eventos@hostwise.pt" class="btn btn-outline">
        Contacte-nos
        <svg width="18" height="18" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"></path>
          <polyline points="22,6 12,13 2,6"></polyline>
        </svg>
      </a>
    </div>
  </div>
</section>

<style>
  .sponsors {
    background: var(--color-dark);
    position: relative;
  }

  .sponsors-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .sponsors-label {
    display: inline-block;
    color: var(--color-accent);
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 1rem;
  }

  .sponsors-header .section-subtitle {
    margin: 0 auto;
  }

  .sponsors-grid {
    display: grid;
    grid-template-columns: repeat(4, 1fr);
    gap: 2rem;
    margin-bottom: 4rem;
  }

  .sponsor-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2.5rem 2rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    transition: all 0.3s ease;
  }

  .sponsor-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(0, 201, 167, 0.3);
    transform: translateY(-4px);
  }

  .sponsor-card:hover .sponsor-logo img {
    filter: brightness(1.2);
  }

  .sponsor-logo {
    width: 160px;
    height: 50px;
    display: flex;
    align-items: center;
    justify-content: center;
  }

  .sponsor-logo img {
    max-width: 100%;
    max-height: 100%;
    object-fit: contain;
    filter: brightness(0.8);
    transition: filter 0.3s ease;
  }

  .sponsor-name {
    margin-top: 1rem;
    font-size: 0.875rem;
    color: var(--color-gray-500);
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .sponsor-card:hover .sponsor-name {
    opacity: 1;
  }

  .sponsors-cta {
    text-align: center;
    padding: 3rem;
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
  }

  .sponsors-cta p {
    color: var(--color-gray-300);
    font-size: 1.125rem;
    margin-bottom: 1.5rem;
  }

  @media (max-width: 1024px) {
    .sponsors-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .sponsors-grid {
      grid-template-columns: 1fr;
    }

    .sponsor-name {
      opacity: 1;
    }
  }
</style>