---
const speakers = [
  {
    id: 1,
    name: "Ricardo Valente",
    role: "Diretor Geral",
    company: "Savills Porto",
    image: "/images/speakers/ricardo-valente.jpeg",
    bio: "Ricardo Valente é Vereador do Turismo, Comércio, Cultura e Património na Câmara Municipal do Porto. Com vasta experiência em políticas públicas de turismo, tem sido uma voz ativa no debate sobre o equilíbrio entre o crescimento turístico e o planeamento urbano sustentável."
  },
  {
    id: 2,
    name: "Renato Chainho",
    role: "CEO e Co-Founder",
    company: "Host Wise",
    image: "/images/speakers/renato-chainho.webp",
    bio: "Renato Chainho é CEO e fundador da Host Wise, empresa líder em gestão de Alojamento Local em Portugal. Com mais de 620 unidades geridas em 30+ cidades, tem sido pioneiro na aplicação de tecnologia e AI no setor da hospitalidade."
  },
  {
    id: 3,
    name: "André Ferreira",
    role: "CEO",
    company: "Hoteis Moov",
    image: "/images/speakers/andre-ferreira.jpeg",
    bio: "André Ferreira lidera a Moov Hotels, uma das cadeias hoteleiras mais inovadoras de Portugal. A sua experiência em gestão operacional de excelência com eficiência de custos tornou-o uma referência no setor."
  },
  {
    id: 4,
    name: "Gonçalo Cruz",
    role: "Founder",
    company: "The Host",
    image: "/images/speakers/goncalo-cruz.jpg",
    bio: "Gonçalo Cruz é fundador da The Host, uma plataforma dedicada a conectar profissionais do setor de Alojamento Local. É conhecido pelo seu trabalho em criar comunidade e networking no ecossistema da hospitalidade."
  },
  {
    id: 5,
    name: "Diogo Cardoso",
    role: "Revenue Manager",
    company: "Host Wise",
    image: "/images/speakers/diogo-cardoso.jpg",
    bio: "Diogo Cardoso é especialista em Revenue Management e otimização de preços na Host Wise. Com profundo conhecimento em estratégias de pricing dinâmico, tem ajudado dezenas de proprietários a maximizar os seus rendimentos."
  },
  {
    id: 6,
    name: "Diogo China",
    role: "Operations",
    company: "Host Wise",
    image: "/images/speakers/diogo-china.jpg",
    bio: "Diogo China é responsável pelas operações na Host Wise, garantindo que cada hóspede tenha uma experiência excecional. A sua atenção ao detalhe e foco no cliente são fundamentais para o sucesso da empresa."
  }
];
---

<section class="speakers section" id="speakers">
  <div class="container">
    <div class="speakers-header">
      <span class="speakers-label">Oradores</span>
      <h2 class="section-title">Speakers</h2>
      <p class="section-subtitle">
        Conheça os especialistas que vão partilhar o seu conhecimento
      </p>
    </div>

    <div class="speakers-grid">
      {speakers.map((speaker) => (
        <button class="speaker-card" data-speaker={JSON.stringify(speaker)}>
          <div class="speaker-image">
            <img src={speaker.image} alt={speaker.name} />
            <div class="speaker-overlay">
              <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
                <circle cx="12" cy="12" r="10"></circle>
                <line x1="12" y1="8" x2="12" y2="16"></line>
                <line x1="8" y1="12" x2="16" y2="12"></line>
              </svg>
              <span>Ver Perfil</span>
            </div>
          </div>
          <div class="speaker-info">
            <h3 class="speaker-name">{speaker.name}</h3>
            <p class="speaker-role">{speaker.role}</p>
            <p class="speaker-company">{speaker.company}</p>
          </div>
        </button>
      ))}
    </div>
  </div>

  <!-- Modal -->
  <div class="modal" id="speakerModal">
    <div class="modal-backdrop"></div>
    <div class="modal-content">
      <button class="modal-close" aria-label="Fechar">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <line x1="18" y1="6" x2="6" y2="18"></line>
          <line x1="6" y1="6" x2="18" y2="18"></line>
        </svg>
      </button>
      <div class="modal-header">
        <div class="modal-image">
          <img id="modalImage" src="" alt="" />
        </div>
        <div class="modal-info">
          <h3 class="modal-name" id="modalName"></h3>
          <p class="modal-role" id="modalRole"></p>
          <p class="modal-company" id="modalCompany"></p>
        </div>
      </div>
      <div class="modal-body">
        <p class="modal-bio" id="modalBio"></p>
      </div>
    </div>
  </div>
</section>

<style>
  .speakers {
    background: var(--color-gray-900);
    position: relative;
  }

  .speakers-header {
    text-align: center;
    margin-bottom: 4rem;
  }

  .speakers-label {
    display: inline-block;
    color: var(--color-accent);
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    margin-bottom: 1rem;
  }

  .speakers-header .section-subtitle {
    margin: 0 auto;
  }

  .speakers-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr);
    gap: 2rem;
  }

  .speaker-card {
    background: rgba(255, 255, 255, 0.02);
    border: 1px solid rgba(255, 255, 255, 0.06);
    border-radius: 16px;
    padding: 2rem;
    text-align: center;
    cursor: pointer;
    transition: all 0.3s ease;
    font-family: inherit;
    color: inherit;
  }

  .speaker-card:hover {
    background: rgba(255, 255, 255, 0.05);
    border-color: rgba(0, 201, 167, 0.3);
    transform: translateY(-4px);
  }

  .speaker-card:hover .speaker-overlay {
    opacity: 1;
  }

  .speaker-image {
    position: relative;
    width: 120px;
    height: 120px;
    margin: 0 auto 1.5rem;
    border-radius: 50%;
    overflow: hidden;
    background: linear-gradient(135deg, var(--color-accent), var(--color-accent-dark));
  }

  .speaker-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .speaker-overlay {
    position: absolute;
    inset: 0;
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    gap: 0.5rem;
    background: rgba(0, 201, 167, 0.9);
    color: var(--color-dark);
    font-size: 0.75rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.05em;
    opacity: 0;
    transition: opacity 0.3s ease;
  }

  .speaker-name {
    font-size: 1.25rem;
    font-weight: 700;
    color: var(--color-light);
    margin-bottom: 0.25rem;
  }

  .speaker-role {
    font-size: 0.9rem;
    color: var(--color-accent);
    margin-bottom: 0.25rem;
  }

  .speaker-company {
    font-size: 0.875rem;
    color: var(--color-gray-400);
  }

  /* Modal Styles */
  .modal {
    position: fixed;
    inset: 0;
    z-index: 1000;
    display: flex;
    align-items: center;
    justify-content: center;
    padding: 1.5rem;
    opacity: 0;
    visibility: hidden;
    transition: all 0.3s ease;
  }

  .modal.active {
    opacity: 1;
    visibility: visible;
  }

  .modal-backdrop {
    position: absolute;
    inset: 0;
    background: rgba(0, 0, 0, 0.8);
    backdrop-filter: blur(10px);
  }

  .modal-content {
    position: relative;
    background: var(--color-gray-900);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 20px;
    padding: 2.5rem;
    max-width: 500px;
    width: 100%;
    transform: scale(0.9) translateY(20px);
    transition: transform 0.3s ease;
  }

  .modal.active .modal-content {
    transform: scale(1) translateY(0);
  }

  .modal-close {
    position: absolute;
    top: 1rem;
    right: 1rem;
    background: rgba(255, 255, 255, 0.05);
    border: none;
    border-radius: 50%;
    width: 40px;
    height: 40px;
    display: flex;
    align-items: center;
    justify-content: center;
    color: var(--color-gray-400);
    cursor: pointer;
    transition: all 0.3s ease;
  }

  .modal-close:hover {
    background: rgba(255, 255, 255, 0.1);
    color: var(--color-light);
  }

  .modal-header {
    display: flex;
    align-items: center;
    gap: 1.5rem;
    margin-bottom: 1.5rem;
    padding-bottom: 1.5rem;
    border-bottom: 1px solid rgba(255, 255, 255, 0.1);
  }

  .modal-image {
    width: 80px;
    height: 80px;
    border-radius: 50%;
    overflow: hidden;
    flex-shrink: 0;
  }

  .modal-image img {
    width: 100%;
    height: 100%;
    object-fit: cover;
  }

  .modal-name {
    font-size: 1.5rem;
    font-weight: 700;
    color: var(--color-light);
    margin-bottom: 0.25rem;
  }

  .modal-role {
    font-size: 1rem;
    color: var(--color-accent);
    margin-bottom: 0.25rem;
  }

  .modal-company {
    font-size: 0.9rem;
    color: var(--color-gray-400);
  }

  .modal-bio {
    color: var(--color-gray-300);
    font-size: 1rem;
    line-height: 1.8;
  }

  @media (max-width: 1024px) {
    .speakers-grid {
      grid-template-columns: repeat(2, 1fr);
    }
  }

  @media (max-width: 640px) {
    .speakers-grid {
      grid-template-columns: 1fr;
    }

    .modal-header {
      flex-direction: column;
      text-align: center;
    }

    .modal-content {
      padding: 2rem 1.5rem;
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const modal = document.getElementById('speakerModal');
    const modalBackdrop = modal?.querySelector('.modal-backdrop');
    const modalClose = modal?.querySelector('.modal-close');
    const speakerCards = document.querySelectorAll('.speaker-card');

    const modalImage = document.getElementById('modalImage') as HTMLImageElement;
    const modalName = document.getElementById('modalName');
    const modalRole = document.getElementById('modalRole');
    const modalCompany = document.getElementById('modalCompany');
    const modalBio = document.getElementById('modalBio');

    function openModal(speaker: any) {
      if (modal && modalImage && modalName && modalRole && modalCompany && modalBio) {
        modalImage.src = speaker.image;
        modalImage.alt = speaker.name;
        modalName.textContent = speaker.name;
        modalRole.textContent = speaker.role;
        modalCompany.textContent = speaker.company;
        modalBio.textContent = speaker.bio;
        modal.classList.add('active');
        document.body.style.overflow = 'hidden';
      }
    }

    function closeModal() {
      if (modal) {
        modal.classList.remove('active');
        document.body.style.overflow = '';
      }
    }

    speakerCards.forEach(card => {
      card.addEventListener('click', () => {
        const speakerData = card.getAttribute('data-speaker');
        if (speakerData) {
          const speaker = JSON.parse(speakerData);
          openModal(speaker);
        }
      });
    });

    modalBackdrop?.addEventListener('click', closeModal);
    modalClose?.addEventListener('click', closeModal);

    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') {
        closeModal();
      }
    });
  });
</script>