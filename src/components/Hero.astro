---
---

<section class="hero">
  <div class="hero-background">
    <img src="/images/casa-musica.jpg" alt="Casa da Música" class="hero-image" />
    <div class="hero-overlay"></div>
  </div>
  
  <div class="hero-content">
    <div class="hero-badge animate-fadeInUp">
      <span class="badge-text">Evento Exclusivo</span>
    </div>
    
    <div class="hero-logo animate-fadeInUp delay-1">
      <img src="/images/boost-hospitality.webp" alt="Boost Hospitality" />
    </div>
    
    <h1 class="hero-title animate-fadeInUp delay-2">
      Impulsione o seu negócio de <span class="highlight">Alojamento</span>
    </h1>
    
    <p class="hero-subtitle animate-fadeInUp delay-3">
      Um dia de networking, insights e estratégias para transformar a sua gestão hoteleira e gestão de alojamento local.
    </p>
    
    <div class="hero-details animate-fadeInUp delay-4">
      <div class="detail-item">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <rect x="3" y="4" width="18" height="18" rx="2" ry="2"></rect>
          <line x1="16" y1="2" x2="16" y2="6"></line>
          <line x1="8" y1="2" x2="8" y2="6"></line>
          <line x1="3" y1="10" x2="21" y2="10"></line>
        </svg>
        <span>25 de Março de 2026</span>
      </div>
      <div class="detail-item">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <circle cx="12" cy="12" r="10"></circle>
          <polyline points="12 6 12 12 16 14"></polyline>
        </svg>
        <span>09h00 - 13h00</span>
      </div>
      <div class="detail-item">
        <svg width="24" height="24" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
          <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"></path>
          <circle cx="12" cy="10" r="3"></circle>
        </svg>
        <span>Casa da Música, Sala 2 - Porto</span>
      </div>
    </div>
    
    <a href="#inscricao" class="btn btn-primary hero-cta animate-fadeInUp delay-5">
      Inscreva-se Agora
      <svg width="20" height="20" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2">
        <line x1="5" y1="12" x2="19" y2="12"></line>
        <polyline points="12 5 19 12 12 19"></polyline>
      </svg>
    </a>
  </div>
  
  <div class="scroll-indicator">
    <span>Scroll</span>
    <div class="scroll-line"></div>
  </div>
</section>

<style>
  .hero {
    position: relative;
    min-height: 100vh;
    display: flex;
    align-items: center;
    justify-content: center;
    overflow: hidden;
  }

  .hero-background {
    position: absolute;
    inset: 0;
    z-index: 0;
  }

  .hero-image {
    width: 100%;
    height: 100%;
    object-fit: cover;
    object-position: center;
  }

  .hero-overlay {
    position: absolute;
    inset: 0;
    background: linear-gradient(
      135deg,
      rgba(10, 10, 10, 0.95) 0%,
      rgba(10, 10, 10, 0.8) 50%,
      rgba(10, 10, 10, 0.7) 100%
    );
  }

  .hero-content {
    position: relative;
    z-index: 1;
    text-align: center;
    padding: 2rem;
    max-width: 900px;
  }

  .hero-badge {
    display: inline-block;
    margin-bottom: 2rem;
    opacity: 0;
  }

  .badge-text {
    display: inline-block;
    padding: 0.5rem 1.5rem;
    background: rgba(0, 201, 167, 0.15);
    border: 1px solid var(--color-accent);
    color: var(--color-accent);
    font-size: 0.875rem;
    font-weight: 600;
    text-transform: uppercase;
    letter-spacing: 0.1em;
    border-radius: 50px;
  }

  .hero-logo {
    max-width: 280px;
    margin: 0 auto 2rem;
    opacity: 0;
  }

  .hero-logo img {
    width: 100%;
    filter: invert(1);
  }

  .hero-title {
    font-size: clamp(2rem, 5vw, 3.5rem);
    font-weight: 800;
    margin-bottom: 1.5rem;
    color: var(--color-light);
    opacity: 0;
  }

  .hero-title .highlight {
    color: var(--color-accent);
    position: relative;
  }

  .hero-subtitle {
    font-size: clamp(1rem, 2vw, 1.25rem);
    color: var(--color-gray-300);
    margin-bottom: 2.5rem;
    max-width: 600px;
    margin-left: auto;
    margin-right: auto;
    opacity: 0;
  }

  .hero-details {
    display: flex;
    flex-wrap: wrap;
    justify-content: center;
    gap: 1.5rem;
    margin-bottom: 3rem;
    opacity: 0;
  }

  .detail-item {
    display: flex;
    align-items: center;
    gap: 0.75rem;
    padding: 0.75rem 1.25rem;
    background: rgba(255, 255, 255, 0.05);
    border: 1px solid rgba(255, 255, 255, 0.1);
    border-radius: 8px;
    font-size: 0.95rem;
    backdrop-filter: blur(10px);
  }

  .detail-item svg {
    color: var(--color-accent);
    flex-shrink: 0;
  }

  .hero-cta {
    opacity: 0;
    font-size: 1.1rem;
    padding: 1.25rem 2.5rem;
  }

  .scroll-indicator {
    position: absolute;
    bottom: 2rem;
    left: 50%;
    transform: translateX(-50%);
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 0.5rem;
    color: var(--color-gray-400);
    font-size: 0.75rem;
    text-transform: uppercase;
    letter-spacing: 0.15em;
    animation: fadeIn 1s ease 1.5s forwards;
    opacity: 0;
  }

  .scroll-line {
    width: 1px;
    height: 40px;
    background: linear-gradient(to bottom, var(--color-accent), transparent);
    animation: scrollPulse 2s ease-in-out infinite;
  }

  @keyframes scrollPulse {
    0%, 100% {
      transform: scaleY(1);
      opacity: 1;
    }
    50% {
      transform: scaleY(0.5);
      opacity: 0.5;
    }
  }

  @media (max-width: 768px) {
    .hero-details {
      flex-direction: column;
      align-items: center;
    }

    .detail-item {
      width: 100%;
      max-width: 300px;
      justify-content: center;
    }
  }
</style>
